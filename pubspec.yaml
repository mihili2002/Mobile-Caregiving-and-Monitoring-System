name: mobile_caregiving_and_monitoring_system
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.10.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.3
  uuid: ^4.4.0

  flutter_secure_storage: ^9.0.0

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.6.12

  # Voice Chatbot dependencies
  speech_to_text: ^7.0.0
  http: ^1.2.2
  flutter_tts: ^4.2.3
  fl_chart: ^0.68.0
  flutter_local_notifications: ^18.0.1
  http_parser: ^4.1.2
  intl: ^0.19.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  timezone: ^0.9.2
  url_launcher: ^6.3.2
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true


